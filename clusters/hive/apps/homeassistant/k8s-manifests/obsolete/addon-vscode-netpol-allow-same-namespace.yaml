apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: addon-vscode-allow-same-namespace
  namespace: homeassistant
spec:
  podSelector:
    matchLabels:
      app: addon-vscode
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {} 