apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: prometheus-pihole-exporter
  namespace: argocd
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: prometheus-pihole-exporter
  project: default
  source:
    path: clusters/hive/apps/prometheus-pihole-exporter/k8s-manifests
    repoURL: 'git@github.com:AndreiGavriliu/homelab.git'
    targetRevision: HEAD
  syncPolicy:
    syncOptions:
    - CreateNamespace=true
    # automated:
    #   prune: true
    #   selfHeal: true